/**
 * To add another email domain follow the domains specification bellow. Add a new entry in the domains array prefixed by a comma. The email domain should be in quotations. An example is provided in the BEFORE and AFTER entries.
 * 'BEFORE_example': ['test_domain.fi'],
 * 'AFTER_example': ['test_domain_fi', 'another_domain.fi']
 */

const domainObject = 
{
    'domains': ['tuni.fi']
};

module.exports = {
    domains:domainObject
};